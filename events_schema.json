{
	"type": "object",
	"properties": {
		"events": {
			"type": "array",
			"items": {
				"type": "object",
		        "properties": {
		          "type": {
		            "type": "string"
		          },
		          "tags": {
		            "type": "array",
		            "items": {
		              "type": "string"
		            }
		          },
		          "timestamp": {
		            "type": "string"
		          },
		          "distinct_id": {
		            "type": "integer"
		          },
		          "session_id": {
		            "type": "string"
		          },
		          "data": {
		            "type": "object",
		            "properties": {
		              "version": {
		                "type": "integer"
		              }
		            },
		            "required": ["version"]
		          }
		        },
		        "required": ["type", "tags", "timestamp", "distinct_id", "session_id"]
            }
		}
	}
}